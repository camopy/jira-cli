// Package agent hosts the `jira agent` command group — schema, guide,
// adf-matrix, and fieldtypes — the authoritative machine-readable contract AI
// coding assistants read before driving the CLI.
package agent
