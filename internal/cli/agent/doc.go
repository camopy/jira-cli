// Package agent hosts the registry commands mounted under docent's
// `jira agent` group — adf-matrix and fieldtypes, the machine-readable
// ADF and custom-field support registries. The guide and schema
// commands themselves come from docent (see internal/cli/root's agent
// surface wiring and internal/agentguides for the guide content).
package agent
