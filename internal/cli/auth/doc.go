// Package auth hosts the `jira auth` command group for managing Jira
// credentials and the active profile. The credential values live in the backend
// stores (internal/config); these commands never print or transit them.
package auth
