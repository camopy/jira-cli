// Package user hosts the `jira user` command group. Its one subcommand,
// `search`, resolves a display name or email to Jira account identities — the
// step between naming someone and the accountId an ADF mention needs. Read-only
// by design.
package user
