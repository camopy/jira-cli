#!/usr/bin/env bash
set -euo pipefail

# shellcheck source=.mise/tasks/lib/build-env.sh
source .mise/tasks/lib/build-env.sh

mkdir -p "${DIST_DIR:?DIST_DIR is required}"
go build -ldflags "$(jira_ldflags)" -o "$(jira_binary_path)" ./cmd/jira
